/**
 * Base de la API. En desarrollo es relativa y el proxy de ng serve
 * (proxy.conf.json) la redirige al backend .NET; en producción el
 * reverse proxy del hosting cumple el mismo rol.
 */
export const API_BASE = '/api';
